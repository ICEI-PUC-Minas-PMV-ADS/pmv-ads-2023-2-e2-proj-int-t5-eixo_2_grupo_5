@model IEnumerable<app_tech_talent.Models.Usuario>

@{
    ViewData["Title"] = "Index";
}

<h1>Editar sua senha e/ou e-mail</h1>

<div class="table-responsive">
    <table class="table table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>Email</th>
                <th>Senha</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Email)</td>
                    <td>@Html.DisplayFor(modelItem => item.Senha)</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.UsuarioId" class="btn btn-primary btn-sm">Editar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
